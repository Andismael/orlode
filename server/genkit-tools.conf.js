/**
 * Genkit developer tools configuration for Orlode AI.
 *
 * Run the Genkit Dev UI with:
 *   npx genkit start -- npx ts-node-dev --respawn --transpile-only src/index.ts
 *
 * Or for flow inspection only:
 *   npx genkit ui
 */
module.exports = {
  promptDir: './src/genkit/prompts',
};
